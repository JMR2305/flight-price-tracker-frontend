# URL of the FastAPI backend. Omit trailing slash.
# In development with `next dev`, the Next.js rewrite rule in next.config.ts
# proxies /api/* → localhost:8000, so you only need this for a remote backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
